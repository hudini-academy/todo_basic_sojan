{{define "base"}}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel='stylesheet' href='/static/css/main.css'>
    <title>{{block "title" .}}Default Title{{end}}</title>
</head>
<body>
    {{block "header" .}}{{end}}
    <div class="content">
        {{block "content" .}}{{end}}
         {{block "body" .}}{{end}}
    </div>
    {{block "footer" .}}{{end}}
</body>
</html>
{{end}}